# API layer
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4

# Retrieval core
pypdf>=5.0
qdrant-client>=1.12
fastembed>=0.5

# Generation providers
boto3>=1.34
groq>=0.13

# SAP transaction layer (app/mcp/)
mcp>=1.2
httpx>=0.27
# Invoice upload endpoint (multipart file upload) + image handling
python-multipart>=0.0.9
pillow>=10.0

# Sample-data generator (dev convenience only)
fpdf2>=2.8

# Test + evaluation layers (tests/, scripts/eval/). The agent eval deliberately
# adds no dependency of its own: its scorers are plain regex/Decimal checks and
# its language detection is a stopword heuristic, so `pytest` is the only
# addition needed to run all three layers.
pytest>=8.0
